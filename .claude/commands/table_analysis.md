---
description: Find and document ClickHouse, MySQL, or MongoDB tables and collections. Use when the user names a table or collection and wants its purpose / columns / docs under `db-docs/`, or needs data but no `db-docs/` entry fits ("which table has…", "find table", "what table stores…").
---

Load and follow `.cursor/skills/table_analysis/SKILL.md`.
