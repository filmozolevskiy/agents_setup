---
description: Shared database infrastructure for ClickHouse, MySQL, and MongoDB — owns the query CLIs, the schema / collection docs, and the Mongo query mechanics. Use when any task needs to query a database, when the user names a table or collection and wants its purpose / columns / docs, or for any question like "which table has", "find table", "what table stores", "document the X table".
---

Load and follow `.cursor/skills/db_access/SKILL.md`.
