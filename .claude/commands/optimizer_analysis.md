---
description: Analyze Optimizer contestants — per-attempt drill-down or broad matching audit across a content source. Joins MySQL `optimizer_candidates` + `optimizer_attempts` + `optimizer_candidate_tags` with MongoDB `ota.optimizer_logs.fares` to find fares we missed, mistagged, or mispriced.
---

Load and follow `.cursor/skills/optimizer_analysis/SKILL.md`.
