---
description: Correlate a regression window on the genesis repo to the merged PRs most likely to have caused it. Use when "<symptom> started failing at T", "what changed in genesis around T", "what was deployed between T1 and T2", or any regression-triage question that needs a ranked list of recent merges.
---

Load and follow `.cursor/skills/deploy_blamer/SKILL.md`.
