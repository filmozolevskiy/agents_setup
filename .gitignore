# Environment & secrets
.env
.claude/settings.local.json

# Python
__pycache__/
.venv/

# Ephemeral output
reports/
docs/
!.cursor/skills/*/docs/
.cache_workspace/

# Skill eval workspaces (per-iteration run outputs / benchmarks / viewer logs — kept locally, not in git)
.cursor/skills/*-workspace/

# MCP Toolbox binary (downloaded via .cursor/skills/looker/scripts/install_mcp_toolbox.sh)
bin/

# macOS
.DS_Store


# the genesis repo (local checkout owned by the codebase_access skill)
.cursor/skills/codebase_access/codebase/
