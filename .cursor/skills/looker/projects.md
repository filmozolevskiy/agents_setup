# Looker projects registry

The agent only touches Looker projects / GitHub repos listed below. When the
user asks for changes to a project not in the table, stop and propose
adding it before doing any other work.

Each row is **one Looker project ↔ one GitHub repo**. Adding a new project
means adding the row in the same change as the work that introduces it.

## Testing changes to this skill

Use **`looker_skill_smoketest_bookings`** as the test bed for any change
to this skill — new bootstrap-subflow steps, edits to
`references/lookml_best_practices.md`, new manual-handoff blocks, etc.
It is the only project in the registry whose explicit purpose is
"break-things-and-find-out", so:

- Push experimental LookML there, not into a production project.
- Exercise dashboard-edit changes on dashboard **1742**
  ("Looker skill smoketest — Bookings"); revert tile additions you do
  not want to keep.
- The data layer is `ota.bookings`, which is documented under
  `.cursor/skills/db_access/db-docs/mysql/bookings.md`, so numeric
  cross-checks are cheap.

If a change to the skill fundamentally cannot be exercised on this
project (e.g. a feature that only matters for ClickHouse-backed
projects), say so explicitly and propose a separate smoke-test project
before merging.

## Connection constraints

| Connection | Write-capable? | Notes |
|------------|---------------|-------|
| `ota` | **No** | Looker service account is read-only. Confirmed 2026-05-11: no scratch/PDT schema exists on the MySQL instance. `aggregate_table:` PDTs and warehouse-side materialization are both unavailable. Use `persist_with:` (Tier 1 B) as the only viable caching layer. |
| `ota_phoenix` | Unknown | Not yet verified. Run the scratch-schema check before proposing Tier 1 A on `ota_phoenix`-backed explores. |
| `clickhouse-jupiter` | Unknown | Not yet verified. |
| `clickhouse-prod` | Unknown | Not yet verified. Used by `content_integration_-fare_family` (`fare_family` model). Run the scratch-schema check before proposing Tier 1 A on `clickhouse-prod`-backed explores. |

## Active projects

The **GitHub repo basename** and the **Looker project name** are equal
in newly-bootstrapped projects (this skill enforces the rule). For
projects that pre-date the skill the two can drift; both columns are
listed so the agent can navigate either way.

| Name | GitHub repo | Looker project name | Connection | Owner | Purpose |
|------|-------------|---------------------|------------|-------|---------|
| `content_integration_optimizer` | [filmozolevskiy/content_integration_optimizer](https://github.com/filmozolevskiy/content_integration_optimizer) | `content_integration_optimizer` | `ota` | filmozolevskiy | Reference repo for this skill. Optimizer candidates / attempts / bookings analytics. Drives the `Content Integration Optimizer` model in Looker. |
| `looker_skill_smoketest_bookings` | [filmozolevskiy/looker_skill_smoketest_bookings](https://github.com/filmozolevskiy/looker_skill_smoketest_bookings) | `looker_skill_smoketest_bookings` | `ota` | filmozolevskiy | Verification project for this skill. Minimal LookML over `ota.bookings`. Used as the smoke test that the bootstrap subflow works end-to-end. Not a production dashboard. |
| `Optimizer_looker` | [filmozolevskiy/Optimizer_looker](https://github.com/filmozolevskiy/Optimizer_looker) | `optimizer` | `ota` | filmozolevskiy | Optimizer analytics (production). Single model `optimizer`. Repo and Looker project name disagree — repo is `Optimizer_looker`, project is `optimizer`. |
| `looker_tracking_system` | [filmozolevskiy/looker_tracking_system](https://github.com/filmozolevskiy/looker_tracking_system) | `tracking` | `ota` | filmozolevskiy | Tracking / event analytics. Single model `tracking`. |
| `Sherlok_Looker` | [filmozolevskiy/Sherlok_Looker](https://github.com/filmozolevskiy/Sherlok_Looker) | `sherlok` | `clickhouse-jupiter` | filmozolevskiy | Sherlock investigation logs. Single model `sherlok_project` (model name does not match project name; pre-skill convention). |
| `looker_allert_system` | [filmozolevskiy/looker_allert_system](https://github.com/filmozolevskiy/looker_allert_system) | `bookability_allert_system` | `ota`, `ota_phoenix` | filmozolevskiy | Bookability + search alert system. Multi-model: `alerting`, `search`, `bookability_alert`, `search_alert`. "allert" typo is intentional (preserved from original Looker project name). |
| `content_integration_cancellation` | [filmozolevskiy/content_integration_cancellation](https://github.com/filmozolevskiy/content_integration_cancellation) | `cancellation` | multiple (default-all) | filmozolevskiy | Cancellation analytics. Single model `cancellation`. Looker project is currently bound to every connection on the instance — narrow before adding new explores. |
| `route_limiter_XP` | [filmozolevskiy/route_limiter_XP](https://github.com/filmozolevskiy/route_limiter_XP) | `route_limiter_xp` | `ota_phoenix` | filmozolevskiy | Route-limiter experiment analytics. Single model `route_limiter_xp`. |
| `content_integration_search` | [filmozolevskiy/content_integration_search](https://github.com/filmozolevskiy/content_integration_search) | `content_integration_search` | `ota_phoenix` | filmozolevskiy | Search analytics for Content Integration. Single model `content_integration_search`. |
| `content_integration_manual_tasks_visibility` | [filmozolevskiy/content_integration_manual_tasks_visibility](https://github.com/filmozolevskiy/content_integration_manual_tasks_visibility) | `content_integration_manual_tasks_vivibility` | `ota` | filmozolevskiy | Visibility into manual ops tasks. Single model `content_integration_manual_tasks_vivibility`. NOTE: the model file basename has a `vivibility` typo (preserved from the live Looker project name); the repo name is spelled correctly. |
| `content_integration_jupiter_logs` | [filmozolevskiy/content_integration_jupiter_logs](https://github.com/filmozolevskiy/content_integration_jupiter_logs) | `jupiter` | `clickhouse-jupiter` | filmozolevskiy | Looker visibility for the Jupiter project. Repo backs the Looker `jupiter` project (file: `models/jupiter_tags.model.lkml`). Looker [folder 547](https://flighthub.looker.com/folders/547). `get_models` exposes `jupiter` + `ci_jupiter_logs` models on this project to the MCP role; `jupiter_tags` may be gated by role / not have an explore yet. |
| `content_integration_alert_system` | [filmozolevskiy/content_integration_alert_system](https://github.com/filmozolevskiy/content_integration_alert_system) | `contente_itegration_alert` | `ota_phoenix` | filmozolevskiy | Content-Integration alerting block (Datatonic-style anomaly detection on top of `ota_phoenix`). Single model `content_itegration_alert`. Looker [folder 479](https://flighthub.looker.com/folders/479). Looker project name preserves the `contente_itegration_alert` double-typo (one `n` missing in `contente` and `itegration`); do not "fix" it — every dashboard tile keys off this name. Distinct from `looker_allert_system` / `bookability_allert_system` above. |
| `content_integration_ancillaries` | [filmozolevskiy/content_integration_ancillaries](https://github.com/filmozolevskiy/content_integration_ancillaries) | `content_integration_ancillaries` | `ota` | filmozolevskiy | Ancillaries / bags optimizer analytics (new system, Trello [#2435](https://trello.com/c/YLbz944q), genesis PRs #52602 → #52655). Single model `content_integration_ancillaries`, one explore over `ota.ancillaries_optimization_baggage` joined to `ancillaries_optimization_baggage_options`. |
| `content_integration_seats` | [filmozolevskiy/content_integration_seats](https://github.com/filmozolevskiy/content_integration_seats) | `content_integration_seats` | `ota` | Lauren | Ancillary (seats) tracking. Backs [dashboard 1577](https://flighthub.looker.com/dashboards/1577). Single model `seats` → one explore `content_integration_seats` over a single `derived_table` joining `booking_ancillaries_tracking` + `..._segment` + `..._segment_meta`. Looker pull+deploy from production landed in repo 2026-05-15. |
| `content_integration_routehappy` | [Dieste1/routehappy_coverage](https://github.com/Dieste1/routehappy_coverage) | `content_integration_routehappy` | `ota` | Dieste1 | RouteHappy coverage analytics. Backs [dashboard 1721](https://flighthub.looker.com/dashboards/1721). Single model `routehappy` → one explore `optimizer_attempt_bookings` joining bookings + optimizer candidates + RouteHappy documents. Default branch is `master` (not `main`). Repo basename (`routehappy_coverage`) and Looker project name (`content_integration_routehappy`) intentionally differ — the Looker project is already wired to this repo. |
| `content_integration_-fare_family` | [filmozolevskiy/content_integration_-fare_family](https://github.com/filmozolevskiy/content_integration_-fare_family) | `fare_family` | `clickhouse-prod`, `ota` | filmozolevskiy | Fare family analytics ([Looker dashboard 1518](https://flighthub.looker.com/dashboards/1518)). Migrated from an external company-git repo to this user-owned GitHub repo via a two-parent merge commit bridging the orphan `master` (Looker bootstrap) and dev branch histories (master tip `079fe6c3`, 2026-05-12). Multi-model: `fare_family` on `clickhouse-prod`, `mysql_model` on `ota`. Repo name has a stray `_-` between `integration` and `fare_family`; preserved as-is because GitHub URL is already wired into Looker's Configure Git — do not "fix" without explicit ask. Note: `get_models` still returns a third model `fara_family` on `clickhouse-prod` — confirmed 2026-05-12 to be a stale duplicate held only in Looker's in-memory production state, intentionally not ported. It drops cleanly at the first Pull+Deploy from this repo; do not re-add it. |
| `content_integration_checkout_interactions` | [filmozolevskiy/content_integration_checkout_interactions](https://github.com/filmozolevskiy/content_integration_checkout_interactions) | `content_integration_checkout_interactions` | `ota` | filmozolevskiy | Customer-side checkout interaction error breakdown over `ota.bookability_checkout_interactions` (~13.2M rows). Single model `content_integration_checkout_interactions`, one explore. Error-type / category trends plus dedicated validation-error detail (`validation_fields` / `validation_values` / `validation_messages`, extracted from `payload.user_error_display`). `ota` read-only → 1h query-result cache via `checkout_interactions_default` datagroup, no PDT. Initial scaffold commit `dcb9431`, 2026-06-26; default branch `main`. Not yet connected in Looker. |
| `content_integration_booking_rules_shadow` | [filmozolevskiy/content_integration_booking_rules_shadow](https://github.com/filmozolevskiy/content_integration_booking_rules_shadow) | `content_integration_booking_rules_shadow` | `ota` | filmozolevskiy | Booking-rules shadow visibility: successful `bookability_contestant_attempts` (last window, `status = 1`) inner-joined to `booking_rules_shadow` on `search_hash = search_id AND package_hash = package_id`, exposing the Amadeus-vs-RouteHappy refund / exchange mismatch flags, the `rules_comparison` / `rules` / `rules_routehappy` JSON payloads, and a clickable debug-logs deep link. Single model `content_integration_booking_rules_shadow`, one explore on `bookability_contestant_attempts`. Join is `many_to_many` (shadow not unique per search+package; attempts share search+package) so all counts are `count_distinct` on each PK. `ota` read-only. Connected + deployed 2026-08-11; scaffold commit `7900d77`, 30-day default `04fe65a`; default branch `main`. **Gotcha:** on first connect, Looker set the project's **Git Production Branch Name to `master`** while the repo uses `main`, so production served an empty branch and non-dev users / the API account got "Model is not configured" (dev mode worked because it reads `main`). Fix was Project Settings → Configuration → set **Git Production Branch Name = `main`**, Save, then Deploy to Production. Verified via Looker `query` vs DB (30d: ~1,664 attempts, ~48.3% mismatch rate). **Extended 2026-08-12 (HEAD `c2944ad`):** added `booking_details` join (`left_outer many_to_one` on `booking_id`, UNIQUE key so no fan-out) exposing `is_upgraded_package` / `upgrade_type` / `upgrade_source_page`; and a `rules_comparison_dimension` derived-table view that unnests `rules_comparison` (master + slave arrays via `JSON_TABLE`) into one row per shadow × pax × rule dimension (`refund_before_departure`, `refund_after_departure`, `exchange_before_departure`, `exchange_after_departure`), with parsed `amadeus`/`routehappy` serviceable + penalty columns and `count_distinct` measures (`left_outer one_to_many` on `shadow_id`). Also `bookability_contestant_attempts.booking_link` (Respro `/booking/index/<booking_id>` deep link). Dashboard [1780](https://flighthub.looker.com/dashboards/1780) drives these with `Attempt Created Date` + `Is Upgraded Package` filters. Base table `booking_rules_shadow` is a rolling ~2-day log (~66k rows) so the inline derived table needs no persistence. **Extended 2026-08-12 (HEAD `e4e2c8d`):** added `booking_fare_rules` join (`left_outer one_to_many` on `booking_id`, NOT unique: 43.1M rows / 26.1M bookings, ~1.65 per booking) exposing only the `rule` text field (all other columns hidden). **Extended 2026-08-12 (HEAD `92f250d`):** made `booking_fare_rules.rule` a click-to-drill cell — its `html:` is an 80-char preview wrapped in `<a href="#drillmenu" target="_self">` with `drill_fields: [booking_id, airline_code, rule_full]`, so clicking a Fare Rule Text cell opens Looker's native drill overlay showing the full text. Full text rendered by a new hidden `rule_full` dimension (same `${TABLE}.rule`, `white-space: pre-wrap` monospace html). Rendering only; value unchanged. Drill filters on the rule text, so bookings with byte-identical rules list together in the overlay. **Extended 2026-08-12 (HEAD `d4d8721`):** `rule_full` sql now reformats ATPCO text for readability — `REGEXP_REPLACE(CONVERT(rule USING utf8mb4), '[ ]*-{3,}[ ]*', '\n\n')` turns dashed divider runs into paragraph breaks, then collapses `[\r\n]{3,}` to a single blank line. utf8mb4 CONVERT is required (column collation `utf8mb3_unicode_ci`/`binary` mismatch rejects raw REGEXP_REPLACE). `-{3,}` avoids the arrow format's `-->` (two hyphens). Display-only; hidden field. **Extended 2026-08-12 (HEAD `2579519`):** the drill overlay collapses whitespace and strips inline `white-space:pre-wrap`, so real `\n` newlines rendered as one wall (verified via Looker `query`: deployed value carried `\n` but overlay showed flat text). Fixed by emitting `<br>` tags instead — `rule_full` now HTML-escapes `&`/`<`/`>`, converts `-{3,}` divider runs to `\n\n`, drops `\r`, converts `\n` to `<br>`, then collapses `(<br>){3,}` to `<br><br>`. `<br>` survives the overlay sanitizer; structural tags render but inline `style` does not. **Extended 2026-08-12 (HEAD `5e2fd60`):** WestJet flat-format rules ("PE PENALTIES FARE BY RULE …") use Unicode dash divider runs (en/em dash) and carry no newlines, so ASCII-only `-{3,}` missed them and they stayed a wall. Broadened the divider class to `[-` U+2010 U+2011 U+2012 U+2013 U+2014 U+2015 U+2212 `_=]{3,}` (verified the pushed file's UTF-8 bytes). Two-hyphen arrows (`-->`) stay intact (`{3,}`). Divider char confirmed via Looker `query` on deployed `rule_full` (en-dash runs survived pre-fix). **Reworked 2026-08-12 (HEAD `795f993`):** dropped the click-drill approach entirely — the drill overlay collapses whitespace and stripped inline styles, so no formatting survived (multiple deploy cycles proved it). Switched to a **native `title` hover tooltip** on `rule`: sql HTML-escapes `&`/`<`/`>`/`"`, converts Unicode/ASCII divider runs and real newlines to `&#10;` newline entities (which the browser renders as line breaks in the tooltip, outside Looker's CSS), and collapses `(&#10;){3,}`. `html` is `<span title="{{ value }}">…</span>` with a Liquid `replace`-chain producing a clean 90-char single-line preview in the cell. Removed `rule_full` and `drill_fields`. Fully flat rules (no dividers/newlines, e.g. some DL rules) still show as one wrapped paragraph. Fallback if the sanitizer strips `title`: styled CSS hover panel. |
| `content_integration_seat_fulfillment` | [filmozolevskiy/content_integration_seat_fulfillment](https://github.com/filmozolevskiy/content_integration_seat_fulfillment) | `content_integration_seat_fulfillment` | `ota` | filmozolevskiy | Seat fulfillment rate (seats confirmed ÷ seats requested on ticketed, non-voided bookings) by booking date, airline, service code (X-SEAT / F-SEAT) and seat type (free / paid). Single model `content_integration_seat_fulfillment`, one explore over a single native derived table (no base table exists — the whole metric is a 4-CTE aggregation). `ota` is read-only so it is a plain NDT, query-result-cached via the `seat_fulfillment_default` daily datagroup at the explore — no PDT. Dashboard is a Looker object built via the MCP — [dashboard 1747](https://flighthub.looker.com/dashboards/1747) "Seat Fulfillment Rates" in [folder 568](https://flighthub.looker.com/folders/568), 4 tiles (Raw Data, Chart, Quarterly review, Daily - Total); not stored as LookML. **Private repo** — Looker needs a deploy key to connect. Distinct data source from the pre-existing `content_integration_seats` (that one reads `booking_ancillaries_tracking`; this reads `booking_ancillaries_history`). Initial scaffold commit `7cedc2a`, 2026-06-02. |

## Adding a new project

1. Pick a short, snake_case name. The Looker project name, the GitHub repo
   name, and the registry entry should match.
2. Stand up the repo via the bootstrap subflow
   (`references/bootstrap_lookml_repo.md`).
3. Add a row above with: name, repo URL, Looker project name, connection,
   owner, one-sentence purpose.
4. Open the PR. Keep registry edits in the same PR as the bootstrap so a
   reviewer sees both legs.

## Removing / archiving a project

- Move the row to an `## Archived projects` section at the bottom of this
  file (do not silently delete).
- Note the date and reason in one short line.
- The agent must refuse work on archived entries until the user re-activates
  them explicitly.
